# HD CyCIF NOX1 analysis pipeline — pinned versions (v5).
#
# TWO environments are used (see README.md):
#
# (A) De-seaming  [pipeline/1_deseam_refuse.py]  — Python 3.11
BaSiCPy==2.0.0
jax==0.10.1
#
# (B) Segmentation / typing / analysis  [pipeline/3b,4b,2b,7,8,9]  — Python 3.11, NVIDIA GPU
cellpose==4.1.1          # Cellpose-SAM
# PyTorch with CUDA must be installed separately to match your GPU/driver, e.g.:
#   pip install torch==2.12.0 --index-url https://download.pytorch.org/whl/cu130
torch==2.12.0            # +cu130 build (CUDA 13.0); install from the PyTorch CUDA index
scikit-image==0.26.0
scikit-learn==1.9.0
scipy==1.17.1
numpy==2.4.6
pandas==2.3.3
matplotlib==3.10.9
tifffile==2026.3.3
openpyxl>=3.1
#
# The v5 segmentation/typing scripts also import the companion `cynif` package
# (segmentation, features, utils) — install it on the PYTHONPATH (not on PyPI).
